export interface ReaderThemes {
  mainBgColor: string;
  mainTextColor: string;
}

export const light: ReaderThemes = {
  mainBgColor: "white",
  mainTextColor: "black",
};

export const themes = {
  light: light,
};
